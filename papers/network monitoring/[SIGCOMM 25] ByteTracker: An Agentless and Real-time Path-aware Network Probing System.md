# ByteTracker: An Agentless and Real-time Path-aware Network Probing System

## 기본 정보
| 항목 | 내용 |
|------|------|
| **제목** | ByteTracker: An Agentless and Real-time Path-aware Network Probing System |
| **저자** | Shixian Guo, Kefei Liu, Yulin Lai, Yangyang Bai, Ziwei Zhao, Songlin Liu, Jianghang Ning, Gen Li, Jianwei Hu, Yongbin Dong, Feng Luo, Sisi Wen, Qi Zhang, Yuan Chen, Jiale Feng, Yang Bai, Chengcai Yao, Zhe Liu, Xin Hu, Yang Lv, Zhuo Jiang, Jiao Zhang, Tao Huang |
| **소속** | ByteDance China / BUPT (State Key Lab of Networking and Switching Technology) / Purple Mountain Laboratories |
| **학회** | ACM SIGCOMM 2025 (September 8–11, 2025, Coimbra, Portugal) |
| **DOI** | 10.1145/3718958.3750515 |
| **키워드** | Network Diagnosis, Agentless Probing, Real-time Path Tracking |

---

## 연구 배경 및 문제

### Pingmesh 중심 시스템의 한계
기존 데이터센터 네트워크 모니터링·진단 시스템은 **Pingmesh**에 기반하며, 서버 간 능동 프로빙으로 RTT와 패킷 드롭률을 측정한다. 그러나 장기 배포 과정에서 다음과 같은 근본적 한계가 드러남:

| 한계 | 설명 |
|------|------|
| **높은 프로브 노이즈** | 엔드호스트의 CPU 과부하 등으로 타임아웃 응답 빈발 → 알림 임계값 상향 불가피 → **0.01% 미만의 미세 패킷 드롭**(결함 스위칭 칩)이 노이즈에 매몰되어 감지 불가 (Figure 1) |
| **호스트/네트워크 타임아웃 구별 불가** | 타임아웃이 호스트 문제(재부팅, CPU 과부하)인지 네트워크 문제(링크 플래핑, 패킷 손상)인지 구분할 수 없어 장애 위치 파악에 노이즈 간섭 |
| **경로 추적의 느린 속도·낮은 정확도** | 이상 프로브의 5-tuple을 수집 후 Traceroute/INT로 경로 재현 → 수 분 소요(분산 학습 등 장애 민감 서비스에 허용 불가). 경로 변경이나 패킷/flowlet 수준 부하 분산 시 재현 경로와 실제 경로 불일치 |
| **Silent bit flipping 미탐지** | 장치 장애로 전송 중 비트 반전 발생 가능. CRC·TCP checksum을 모두 통과하는 극단 사례가 존재하여 서비스 크래시 유발. 기존 프로빙 시스템은 비트 반전을 탐지·위치 파악할 수 없음 |
| **에이전트 의존성** | 수백만 서버 각각에 user-space 에이전트 배포·업데이트·관리 필요. 에이전트 버그로 프로빙 빈도가 비정상 증가 시 네트워크에 대량 프로브 패킷 유입 → 즉시 중단이 어려움 |

### 프로빙 시스템의 3가지 핵심 수요

| 수요 | 상세 |
|------|------|
| **Demand-1: 미세 패킷 드롭 탐지·위치 파악** | 고위 스위치의 0.01% 미만 패킷 드롭도 감지하여 서비스 성능 저하 원인을 파악 |
| **Demand-2: 빠르고 정확한 경로 추적** | 장애 발생 시 실시간으로 프로브 경로를 파악하여 5초 이내 장애 위치 파악 |
| **Demand-3: Silent bit flipping 위치 파악** | 전송 중 비트 반전이 발생한 정확한 스위치를 식별 |

---

## ByteTracker 시스템 개요

### 설계 목표
1. **에이전트리스(Agentless)**: 엔드호스트에 프로브 에이전트·설정 변경 불필요
2. **높은 제어성**: 소수의 중앙집중형 Prober로 모든 프로브를 정밀 제어. 위험 발생 시 즉시 중단 가능
3. **낮은 프로브 노이즈 + 타임아웃 분류**: 호스트/네트워크 타임아웃을 정확히 구분
4. **정확하고 실시간인 경로 추적**: ERSPAN 패킷 미러링으로 프로브 경로를 실시간 추적
5. **낮은 오버헤드 + 쉬운 배포**: 배포·프로브 생성·대역폭·호스트 리소스 사용 최소화

### 핵심 아이디어

```
┌─────────────────────────────────────────────────────────────────┐
│                        ByteTracker Framework                     │
│                                                                   │
│  ① Prober (DCI 스위치에 연결된 서버)                                │
│     - TCP SYN (무효 목적지 포트, 512B 페이로드) → 타겟 서버           │
│     - 타겟 서버 커널이 자동으로 TCP RST 응답 (에이전트 불필요)         │
│     - 타겟당 3개 프로브를 동시에 전송 → 호스트/네트워크 타임아웃 분류   │
│                                                                   │
│  ② ERSPAN (모든 DC 스위치에서)                                      │
│     - DSCP 값으로 프로브/응답 패킷 식별 → 미러링                     │
│     - 관리 네트워크로 전송 → Analyzer                               │
│                                                                   │
│  ③ Analyzer (원격 분석 서버)                                        │
│     - 실시간 프로브 경로 추적                                       │
│     - 네트워크 타임아웃 프로브의 마지막 미러링 위치로 장애 스위치 마킹  │
│     - 홉별 패킷 내용 비교로 bit flipping 탐지·위치 파악              │
└─────────────────────────────────────────────────────────────────┘
```

---

## 핵심 설계

### 4.1 프로브 데이터 플레인

#### 4.1.1 에이전트리스 프로빙 + 패킷 미러링
- **TCP SYN (무효 포트)** vs **ICMP Ping** 비교 후 TCP 선택
  - ICMP: CPU 소모 더 낮지만 포트 번호 없음 → ECMP 커버리지 부족
  - TCP SYN: 완전한 5-tuple → 소스 포트 변경으로 모든 ECMP 경로 커버
- 타겟 서버 **커널**이 RST로 자동 응답 → user-space 프로세스 대비 높은 처리 우선순위 → 프로브 노이즈 대폭 감소
- SYN 페이로드 **512바이트**: 모든 스위치의 셀 크기(≤256B)를 초과하여 패킷 분할·재조립 과정의 이상도 탐지
- **DSCP** 값으로 프로브/응답 패킷 식별 → ERSPAN이 해당 패킷만 미러링
- 미러링 패킷은 **관리 네트워크**로 전송 → 서비스 네트워크 대역폭 소모 없음

#### 4.1.2 2방향 프로빙 (North-South + East-West)

| 프로빙 유형 | 방식 | 커버리지 |
|-----------|------|---------|
| **North-South** | Prober → (DCI → T2 → T1 → ToR → Server) → RST 반환 | 모든 네트워크 링크, 스위치 포트, **수직 방향(cross-tier) 포워딩 규칙** |
| **East-West** | Prober → IP-in-IP 캡슐화 → ToR 스위치(디캡슐화) → 고위 멀티칩 스위치 경유 → 타겟 서버 → RST 반환 | 멀티칩 스위치의 **수평 방향(intra-tier) 포워딩 규칙** (칩 내부 + 칩 간) |

- **멀티칩 스위치 문제**: 고위 스위치는 여러 스위칭 칩과 스위칭 패브릭으로 구성 → 칩 내부(intra-chip) 포워딩과 칩 간(inter-chip) 포워딩 모두 장애 가능
- North-South만으로는 수직 방향만 커버 → East-West가 수평 방향을 보완
- East-West는 각 Pod에서 ToR 1대만 IP-in-IP 활성화 → 낮은 배포 오버헤드

#### 4.1.3 호스트/네트워크 타임아웃 분류 (동시 프로빙)
- 타겟당 **3개 프로브를 서로 다른 소스 포트로 동시 전송** → 서로 다른 ECMP 경로
- 판정 규칙:
  - **1~2개 타임아웃** → 네트워크 원인 (다수 병렬 경로가 동시 장애할 확률 극히 낮음)
  - **3개 모두 타임아웃** → 호스트 원인 (호스트 문제는 모든 경로에 영향)
- 장점: 단일 타겟의 프로브 데이터만으로 판단 가능 → 추가 메트릭/이벤트/다른 NIC의 프로브 결과 불필요
- **밀리초 수준 정확도**: R-Pingmesh의 ToR-mesh 프로빙(100ms 주기, 다수 NIC 의존)보다 우수

> **예외**: ToR 스위치의 다운스트림 포트/링크 장애 시 모든 프로브가 드롭 → NIC 상태 모니터링으로 탐지 가능하므로 호스트 타임아웃으로 분류

---

### 4.2 프로브 태스크 생성

| 프로빙 유형 | 핑리스트 | 빈도 |
|-----------|---------|------|
| **North-South** | DC 내 모든 NIC + 다른 DC의 모든 Prober | 전체 핑리스트를 초당 약 1회 순회 → NIC당 평균 3 프로브/초 |
| **East-West** | DC 내 모든 Pod 쌍 (각 Pod에서 ToR 1대 활성화) | 초당 각 Pod 쌍 프로빙, 타겟 Pod에서 랜덤 NIC 1개 선택 |

- 에이전트 기반 시스템 대비: 핑리스트 설계·분배·갱신 불필요, Prober 소수만 배포

---

### 4.3 프로브 분석 및 장애 위치 파악

#### 4.3.1 네트워크 패킷 드롭 정확 위치 파악 (Algorithm 1)

**Step 1: 추가 필터링**
- Prober가 네트워크 타임아웃으로 분류한 프로브 중, ToR 스위치가 SYN을 캡처했으나 RST를 어떤 스위치도 캡처하지 못한 경우 → 호스트 타임아웃으로 재분류
- 결과: **거의 순수한 네트워크 타임아웃 프로브** 확보

**Step 2: 마지막 미러링 위치로 장애 스위치 마킹 (MarkSwitches)**
- 각 네트워크 타임아웃 프로브의 마지막 미러링 스위치에 abnormal_cnt 증가
- **abnormal_cnt ≥ 5**이면 해당 스위치를 "이상"으로 마킹
- 오탐 확률: 미러링 패킷 드롭 확률 $p_i$가 각각 1% 미만이면, 5개 독립 프로브가 동시에 잘못 마킹할 확률 $P_j = \prod p_i \ll (0.01)^5 \approx 0$

**Step 3: 다중 스위치 마킹 시 처리 (LocateAbnormalSwitches)**
- 이상 스위치가 1개 → 바로 반환
- 이상 스위치가 >1개:
  - 다음 홉 스위치의 미러링 카운터가 10% 이상 감소한 스위치 존재 → 해당 스위치가 진짜 장애 (Figure 7(c): 결함 칩이 미러링도 실패하는 경우)
  - 감소 없음 → 모든 마킹된 스위치가 실제로 이상 (예: 패치 버그로 다수 스위치 동시 장애)

#### 대부분의 패킷 드롭 패턴 (Figure 7)
- **(b) 일반적 경우**: ERSPAN은 스위치가 포워딩하기 **전에** 미러링 → 포워딩 과정에서 드롭되더라도 미러링은 성공 → 마지막 미러링 위치 = 장애 스위치
- **(c) 극단 경우**: 결함 칩이 포워딩도 실패하고 미러링도 실패 → 이전 홉 스위치들이 마지막 미러링 위치로 다수 마킹 → 다음 홉 미러링 카운터 검사로 해결

#### 4.3.2 Silent Bit Flipping 탐지·위치 파악
- Analyzer가 홉별로 미러링된 프로브 내용을 비교
- TTL, ECN 등 변동 비트를 1로 설정 → **MD5 다이제스트** 계산 → 홉별 비교
- 특정 홉에서 다이제스트 변경 발견 → 해당 홉의 이전 스위치 → 현재 스위치 구간에서 비트 반전 발생

---

## 배포 경험

### 단계적 배포 (총 1년)

| 단계 | 기간 | 내용 |
|------|------|------|
| **Phase-1** | 3개월 | 단일 DC에 North-South 프로빙 + ERSPAN 배포. 에이전트리스 프로빙, 동시 프로빙 타임아웃 분류, 경로 추적, 장애 위치 알고리즘 검증. 결함 칩의 미러링 실패 사례 발견 → Algorithm 1 개선 |
| **Phase-2** | 3개월 | 동일 DC에 East-West 프로빙 추가. Pingmesh와 지속 비교하여 장애 위치 정확도 개선. Pingmesh가 탐지하는 모든 문제 + 미세 패킷 드롭·bit flipping 추가 탐지 확인 |
| **Phase-3** | 6개월 | 전체 DC(수백만 서버)에 점진적 배포. ByteTracker를 주 모니터링·진단 시스템으로 전환 |

### ERSPAN 미지원 레거시 클러스터
- **Phase-1**: Prober만 배포하여 N-S/E-W 프로빙 (경로 추적 없이). 에이전트리스 + 낮은 노이즈 + 타임아웃 분류만으로도 Pingmesh 대비 우수
- **Phase-2**: Prober가 중앙집중형으로 Traceroute 실행 → 분산 에이전트 기반 Traceroute보다 세밀한 제어 가능, 네트워크 타임아웃 프로브만 사용하여 더 정확한 장애 위치 파악

---

## 평가 결과 (6개월 배포, 수십만 서버 DC)

### 시스템 오버헤드
- 엔드호스트: NIC당 평균 3 프로브/초 수신 → NIC 대역폭·CPU·메모리 소모 **무시 가능**
- Prober: 프로브 ~600Mbps, 응답 ~100Mbps → DC 전체 스위치·링크에 분산되므로 무시 가능
- 미러링 패킷: 관리 네트워크로 전송 → 서비스 네트워크 대역폭 미소모
- 프로브 1라운드 완료 시간: ~1초 (Figure 8)

### 프로브 노이즈
- CPU 사용률 100% 도달 사례 (Figure 9):
  - Pingmesh: 대량 타임아웃 + tail RTT 급증 (에이전트가 CPU 사이클 확보 실패)
  - ByteTracker: 타임아웃 거의 없음 + tail RTT 안정 (커널 기반 응답의 높은 우선순위)

### 이상 탐지 효율
- 6개월간 ByteTracker: **276건** 네트워크 이상 탐지 vs Pingmesh: **262건**
- 추가 탐지 사례:
  - **미세 패킷 드롭**: Pingmesh에서는 노이즈에 매몰 (Figure 10)
  - **멀티칩 스위치 silent 패킷 드롭**: East-West 프로빙으로 8건 탐지
  - **대형 패킷 드롭**: 512B 프로브(패킷 분할 트리거)로 5건 탐지
  - **Silent bit flipping**: 홉별 내용 비교로 2건 정확 탐지·위치 파악

### 장애 위치 정확도 및 속도
- 276건 전체 네트워크 이상에 대해 **100% 정확한 장애 위치 파악**
- Pingmesh 대비: 수 분 → **5초 이내** (실시간 ERSPAN 경로 추적)

---

## 탐지된 주요 이상 사례

| 이상 유형 | 설명 |
|---------|------|
| **결함 칩의 미러링 실패** | 결함 스위칭 칩이 포워딩 + 미러링 모두 실패 → 다수 이전 홉 스위치가 마킹됨 → 다음 홉 미러링 카운터 급감으로 식별 (Figure 11) |
| **다수 스위치 동시 패킷 드롭** | 패치 버그로 다수 스위치 동시 장애 → Pingmesh는 노이즈 간섭으로 오진단, ByteTracker는 순수 네트워크 타임아웃으로 정확 식별 |
| **트래픽 루프** | L3 루프(TTL 소진) 및 L2 루프(무한 순환) → Pingmesh는 타임아웃만 감지, ByteTracker는 실시간 경로 추적으로 루프 직접 확인 |
| **단방향 패킷 드롭** | 대부분의 패킷 드롭이 한 방향에서만 발생 (광모듈 송신기/수신기, 다중 코어 광섬유의 한쪽 손상) |

---

## 운영 교훈

### 멀티칩 스위치 사용 축소
- 멀티칩 스위치는 칩 내부+칩 간 포워딩으로 단일칩 대비 장애 확률 높음
- 스위칭 칩 발전으로 단일칩 대역폭 증가 → 단일칩 스위치로 더 많은 서버 연결 가능 → DC 추세

### 고정 프로브 5-tuple
- 결함 칩의 미러링 실패 시 다수 스위치 오마킹 문제 → 고정 5-tuple 집합으로 경로를 지속 업데이트·저장 → 이력 경로로 장애 위치 보완

### 대형 프로브 패킷 필수
- 스위치의 패킷 분할·재조립 과정의 이상은 셀 크기 이상의 패킷에서만 탐지 가능
- ByteTracker: 512B 페이로드로 모든 스위치의 분할 임계값(≤256B) 초과 보장

### 프로브 재전송 회피
- Pingmesh: 커널 스택의 자동 재전송 → 타임아웃률·RTT 부정확 (재전송 성공 시 타임아웃 미기록, RTT에 대기 지연 포함)
- ByteTracker: raw socket API로 프로브 1회만 전송 → 정확한 측정

### Bit flipping 빈도 증가
- VXLAN 터널 엔트리·게이트웨이가 성능 우선으로 패킷 검증 없이 캡슐화/디캡슐화 → 손상 패킷이 정상으로 전달
- 터널 엔트리·게이트웨이의 패킷 검증 강화 필요

### IP-in-IP 프로브의 한계
- IP-in-IP 프로브는 외부 IP가 스위치 IP(서버 서브넷과 다름) → 서비스 패킷의 포워딩 이상을 탐지하지 못할 수 있음
- ByteTracker는 서버 IP를 타겟으로 사용하여 서비스 패킷 포워딩 이상 탐지 보장

---

## 한계 및 향후 과제

| 한계/향후 과제 | 설명 |
|-------------|------|
| **스위치 포트 수준 위치 파악** | 현재 스위치 단위까지만 위치 파악 → 최신 ERSPAN 버전이 포트 식별 지원 → 포트 수준 장애 위치 가능 |
| **호스트 타임아웃 활용** | 네트워크 타임아웃만 활용 중 → 호스트 타임아웃에서 노이즈를 필터링하여 이상 호스트·NIC 탐지 계획 |
| **정확한 RTT로 핫스팟 탐지** | 커널 기반 응답으로 실제 네트워크 RTT에 근접한 측정 → high RTT + 경로 추적으로 네트워크 성능 병목 위치 파악 |
| **RoCE 클러스터 적응** | TCP 프로브는 RoCE 큐 고유 문제(PFC 데드락 등) 미탐지 → RoCE 큐 전용 프로브 추가 계획 |

---

## 핵심 기여 요약

1. **최초의 에이전트리스 프로빙 시스템**: 수백만 서버 DC에서 엔드호스트 에이전트 없이 중앙집중형 Prober로 모든 프로빙 수행
2. **동시 프로빙 기반 타임아웃 분류**: 타겟당 3개 병렬 프로브로 호스트/네트워크 타임아웃을 밀리초 수준으로 정확 구분
3. **N-S + E-W 2방향 프로빙**: 모든 스위치 포트·링크 + 멀티칩 스위치의 칩 내부·칩 간 포워딩 규칙 완전 커버
4. **ERSPAN 실시간 경로 추적**: 패킷 미러링으로 실시간 정확한 경로 추적 + 홉별 내용 비교로 bit flipping 탐지
5. **검증**: 전체 DC(수백만 서버) 6개월 배포, 276건 이상 탐지, **5초 이내 100% 정확 장애 위치 파악**
